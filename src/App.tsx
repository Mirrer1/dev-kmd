import Users from '@components/Users';
import Subscribe from '@components/Subscribe';

const App = () => {
  return (
    <main>
      <Users />
      <Subscribe />
    </main>
  );
};

export default App;
